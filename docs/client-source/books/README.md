# Kunsang Gar book source material

These PDFs were provided in `~/Downloads/nuevo material kunsang gar/` on 2026-09-21.
They are preserved as private client source only. The originals are blocked from
public web access by this directory's `.htaccess` file.

## Commercial status

`PHASE_2 / STORE MODULE` — not part of V1 México.

Do not publish covers, catalog fichas, purchase URLs, checkout, or store flows
until Geshe approves the store implementation and the corresponding module
advance has been paid. `BUY_LINK_PENDING` is not a V1 pending item; it is
deferred with the entire store module.
