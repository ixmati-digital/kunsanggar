const menuToggle=document.querySelector(".menu-toggle");const navLinks=document.querySelector(".nav-links");if(menuToggle&&navLinks){menuToggle.addEventListener("click",()=>{const open=navLinks.classList.toggle("is-open");menuToggle.setAttribute("aria-expanded",String(open))});navLinks.addEventListener("click",event=>{if(event.target.closest("a")){navLinks.classList.remove("is-open");menuToggle.setAttribute("aria-expanded","false")}})}const backTop=document.querySelector(".back-top");if(backTop){window.addEventListener("scroll",()=>backTop.classList.toggle("is-visible",window.scrollY>500),{passive:true});backTop.addEventListener("click",()=>window.scrollTo({top:0,behavior:"smooth"}))}document.querySelectorAll("a[href],form[action]").forEach(item=>{const attr=item.tagName==="FORM"?"action":"href";const value=item.getAttribute(attr)||"";if(value.startsWith("#"))return;try{const url=new URL(value,window.location.origin);if(url.origin===window.location.origin&&!url.pathname.startsWith("/newplatform/")){item.addEventListener("click",event=>event.preventDefault());item.addEventListener("submit",event=>event.preventDefault())}}catch(error){}});